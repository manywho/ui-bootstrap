/**
 * Describes a single object representing an outcome
 * @todo Need to add the additional properties
 */

interface IOutcomeMetaData {
    isBulkAction?: boolean;
}

export default IOutcomeMetaData;
